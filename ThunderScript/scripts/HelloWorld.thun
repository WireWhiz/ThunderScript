// this is a comment

#in float a
#in float b
#ref float r
#ref bool testBool

/*

Test comment to  make sure the line that the errors 
report is correct

*/

r = 0;
float lastValue = 0;
float limit = 2000000000;
while (!testBool){
	r = r + 1;
	if(lastValue == r)
		testBool = true;
	lastValue = r;
} 